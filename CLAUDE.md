# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This directory is currently empty — no project has been started here yet. There is no build system,
dependency manifest, or source code to describe.

When work begins in this folder, replace this file with real guidance: build/lint/test commands, and
the architecture of whatever gets built here.

## Notes for future instances

- This folder is not (yet) a git repository. If you initialize one here, keep it scoped to this folder
  — do not run `git init` in a parent directory that contains unrelated files (e.g. the user's home
  directory), since that risks accidentally tracking unrelated personal files.
